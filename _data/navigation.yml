# The following is the order of the links in the header of the website.
#
# This minimal homepage keeps a single About page, so the header navigation
# intentionally contains no links. Add entries here if new pages are created.

main: []
